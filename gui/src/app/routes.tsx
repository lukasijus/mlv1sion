// TODO: Define route configuration using react-router-dom (createBrowserRouter/Routes)
// This file will map pages to URLs for auth, projects, datasets, labeling, jobs, admin.

export type RouteConfig = unknown;

export const routes: RouteConfig[] = [];
