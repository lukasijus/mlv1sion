import ReactDOM from 'react-dom/client';
import App from './App';

// TODO: Later wrap with BrowserRouter, QueryClientProvider, ThemeProvider
ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
