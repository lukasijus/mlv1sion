import type { FC } from 'react';
import AppShell from '../layout/AppShell';

// TODO: Root App component; will host Router, QueryClientProvider, ThemeProvider, etc.
const App: FC = () => {
  return <AppShell />;
};

export default App;
