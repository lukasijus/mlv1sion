import type { FC } from 'react';

// TODO: Show a single project's details and datasets
const ProjectDetailPage: FC = () => {
  return <div>ProjectDetailPage</div>;
};

export default ProjectDetailPage;
