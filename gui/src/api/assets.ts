import type { Asset } from './types';

// TODO: Asset API functions (listAssets, getAsset, deleteAsset, etc.)
export async function listAssets(datasetId: string): Promise<Asset[]> {
  throw new Error('Not implemented');
}
